name: Pytest CI with UV
on: [push, pull_request]  # Triggers on push/PR to any branch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install UV (Ultra-Violet)
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH  # Add UV to PATH

      - name: Install dependencies with UV
        run: |
          uv pip install -r requirements.txt pytest pytest-cov

      - name: Run tests
        run: pytest --cov=./ --cov-report=xml  # Generate coverage report

      - name: Upload coverage to Codecov (optional)
        uses: codecov/codecov-action@v3
